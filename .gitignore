# Build artifacts
build/
bin/
*.o

# data/output/ and results/ are intentionally NOT ignored: the assignment
# asks for the generated evidence to be committed alongside the code.

# Staging directory used while building the proof archive
results/.staging/

# The packaged archive is what gets uploaded to the assignment, but every
# file inside it is already committed loose under results/ and data/output/,
# so committing it too would duplicate ~13 MB. Regenerate it with ./run.sh.
results/proof_of_execution.tar.gz

# Python
__pycache__/
*.pyc

# Editor and OS noise
.vscode/
.idea/
.DS_Store
